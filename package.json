{
  "name": "dockerfile_for_nodejs_app",
  "version": "1.0.0",
  "description": "Its a simple dockerfile for running a nodejs app # clone the repo first # Then run docker build command # then run the docker run command with your desired port which you want to map with your container # then search for that port in your search bar with localhost:Port_Number",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
